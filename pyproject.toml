[project]
name = "local_watch"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = ["wegofwd-llm @ git+https://github.com/wegofwd2020-hub/wegofwd-llm@v0.2.0"]
[project.optional-dependencies]
dev = ["pytest", "ruff"]
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
